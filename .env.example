# Frontend environment variables
# Copy to `.env` and adjust as needed.

# Base URL for the backend API.
# Leave empty to use the Vite dev proxy (`/api` -> http://localhost:3001).
# VITE_API_BASE_URL=http://localhost:3001

# Feature flags
# Enable det kommende ressourcemodul (preview-placeholder)
VITE_RESOURCES_ANALYTICS_ENABLED=false
VITE_PROJECT_RISK_ANALYSIS_ENABLED=false

# Logo path served from /public (drop your own png/svg there)
VITE_APP_LOGO_PATH=/branding/logo.png

# -------------------------------------------
# Azure / Microsoft Graph (Password Reset)
# -------------------------------------------
# Required for sending password reset emails via Microsoft Graph API
# AZURE_TENANT_ID=your-tenant-id-here
# AZURE_CLIENT_ID=your-client-id-here
# AZURE_CLIENT_SECRET=your-client-secret-here
# AZURE_MAIL_FROM=noreply@yourorganisation.dk

# -------------------------------------------
# Azure AD SSO (Single Sign-On)
# -------------------------------------------
# Redirect URI must match Azure App Registration
# AZURE_OIDC_REDIRECT_URI=http://localhost:5173/api/auth/azure/callback

# Password Reset
# FRONTEND_URL=http://localhost:5173
# PASSWORD_RESET_TOKEN_EXPIRY_MINUTES=60
